/**
 * Template de js/env-config.js — copie ou gere via `npm run env:generate`.
 * O arquivo real (js/env-config.js) é gitignored; nunca commitar chaves reais aqui.
 */
window.__ENV__ = {
  SUPABASE_URL: 'https://SEU-PROJETO.supabase.co',
  SUPABASE_ANON_KEY: 'SUA-ANON-KEY-PUBLICA',
};
